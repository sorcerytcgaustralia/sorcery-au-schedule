# Files in the repo that are not part of the served site.
# Same syntax as .gitignore; paths are relative to the assets directory
# (the repo root).
.git
.github
.claude
.gitignore
.assetsignore
.nojekyll
node_modules
package.json
package-lock.json
wrangler.jsonc
README.md
HANDOVER.md
chats
# Only project/assets is referenced by the pages; the rest of project/ is
# the original design handoff bundle, kept in the repo for reference.
project/data_raw.json
project/screenshots
project/uploads
project/support.js
project/Sorcery OP Schedule.dc.html
